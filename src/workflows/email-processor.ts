/**
 * Orchestrates the entire flow when Gmail sends a notification
Steps:
Fetch email from Gmail
Parse email (Email Parser Agent)
Find calendar slots (Calendar Agent)
Generate response (Response Generator Agent)
Send email (Gmail Integration)

 */
